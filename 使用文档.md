# 墨阁写作工坊·修复版

## 用户使用文档

**适用版本：v1.6.0**  
**适用程序：Windows 64 位便携版 EXE 与网页版**

墨阁写作工坊是一款面向网络小说创作的本地优先写作软件。作品、章节、人物设定、关系图、统计与备份默认保存在你的电脑中，不需要账号即可使用。只有当你主动配置并调用 AI 辅助写作时，软件才会联网，将当前章节内容及你明确勾选的引用范围发送给所配置的 API 服务。

> 建议：在开始长期创作前，先建立一部测试作品，试用章节编辑、备份恢复与导出功能。确认流程符合习惯后，再导入或创建正式作品。

---

## 1. 快速开始

### 1.1 启动软件

Windows 便携版无需安装。双击 `MogeWriter-v1.6.0-增强功能版.exe` 或本地取得的 EXE 文件即可启动。除 AI 生成功能外，日常写作、设定管理、导出和备份均无需联网。

| 项目 | 说明 |
|---|---|
| 窗口操作 | 顶部标题栏可拖动窗口；右上角提供最小化、最大化与关闭按钮。 |
| 重复启动 | 已有窗口打开时，再次启动会聚焦现有窗口，不会创建多个编辑窗口。 |
| EXE 数据位置 | `%APPDATA%\墨阁写作工坊·修复版`。 |
| 网页版数据位置 | 当前浏览器的 IndexedDB 数据库中，与 EXE 数据相互独立。 |
| 联网需求 | 仅 AI 调用需要联网；是否联网由你是否点击 AI 生成决定。 |

如果使用网页版，直接用 Chrome、Edge 或 Firefox 打开 `index.html` 即可。若浏览器限制本地文件访问，可在源码目录中启动本地服务器后访问，但普通用户建议优先使用 EXE 版。

### 1.2 创建第一部作品

在“书架”页面点击“新建作品”，填写书名、作者、题材、状态、每日目标和简介。作品创建后，进入该作品即可建立卷、章节、大纲和人物设定。

| 建议填写项 | 用途 |
|---|---|
| 书名 | 用于书架识别、导出文件和 AI 当前作品上下文。 |
| 题材与状态 | 便于书架分类管理。 |
| 每日目标 | 用于统计页的今日目标与进度展示。 |
| 简介 | 可作为作品概览；不会自动发送给 AI。 |

---

## 2. 章节与正文写作

### 2.1 建立卷和章节

进入作品后，在章节区域新建卷或章节。章节可以归入指定卷，也可以暂时作为未分卷章节。支持拖拽调整卷与章节顺序，也支持将章节从一个卷移动至另一个卷。章节详情中的“章节标签”可填写多个标签（以逗号分隔），例如“主线、战斗、待修”；左侧章节管理栏会自动汇总标签并支持点击筛选。

建议先建立粗略卷结构，再逐章补充标题和细纲。写作过程中章节排序可随时调整，不会影响已写正文。

### 2.2 编辑器布局

打开章节后进入编辑器。左侧是章节树，中间是正文编辑区，正文上方可显示“本章细纲”和“写作备注”，右侧可选显示 AI 辅助写作侧栏。

| 区域或功能 | 用法与说明 |
|---|---|
| 章节标题 | 可直接在编辑器顶部修改，随自动保存写入本机。 |
| 本章细纲 | 记录本章目标、冲突、转折和结尾钩子。AI 生成时会默认参考。 |
| 写作备注 | 记录人称、语气、伏笔、禁忌或临时写作要求。AI 生成时会默认参考。 |
| 正文编辑区 | 输入小说正文，底部实时显示字数等状态。 |
| 上一章 / 下一章 | 在章节之间快速切换。 |
| 码字模式 | 进入更简洁的沉浸式编辑界面，适合专注写作。 |
| 检查 | 对当前章节执行敏感词和错别字检查。 |
| 历史版本 | 查看章节历史版本、比较内容或安全回滚。 |
| ✨ AI | 显示或隐藏右侧 AI 辅助写作侧栏。 |

### 2.3 保存机制与快捷键

正文、标题、细纲和备注会按“设置”中的自动保存间隔写入本机。关闭窗口时，软件也会尝试保存当前内容；不过重要内容仍建议使用手动备份与 JSON 导出双重保护。

| 快捷键 | 功能 |
|---|---|
| `Ctrl + S` | 立即保存当前章节。 |
| `Ctrl + Enter` | 保存当前章节并跳转至下一章。 |
| `Esc` | 退出码字模式。 |
| `Ctrl + Shift + F` | 打开全局搜索。 |

### 2.4 章节版本历史与回滚

每次产生有效修改并保存后，软件会保留章节版本记录。点击编辑器中的“历史版本”可浏览版本时间、保存原因和正文摘要；比较后可以选择回滚到指定历史版本。

回滚属于会改变当前章节内容的操作。建议在回滚前先确认目标版本内容；软件会保留当前版本的可恢复路径，且全局自动备份也可用于整部作品级别的恢复。

---

## 3. 大纲、设定与剧情管理

### 3.1 大纲文档

“大纲”页面可创建任意数量的大纲文档，例如“总体大纲”“第一卷细纲”“主线时间轴”或“结局安排”。大纲文档适合记录跨章节内容，而章节编辑器中的“本章细纲”只服务于当前章节。

当使用 AI 时，全书大纲**不会默认发送**。只有在 AI 侧栏中主动勾选“引用全书大纲”后，才会以限量摘要形式发送给 API。

### 3.2 人物与世界设定

“设定”页面集中管理人物、势力、地点、物品、功法或能力等内容。人物卡可记录别名、身份、外貌、性格、背景、关系和标签。建议保持名称一致，方便在正文、关系图和 AI 写作中形成稳定上下文。

| 设定类型 | 推荐记录内容 |
|---|---|
| 人物 | 姓名、别名、身份、阵营、性格、外貌、背景、人物关系与标签。 |
| 势力 / 组织 | 名称、成员、目标、资源、敌对或合作关系。 |
| 地点 | 地理位置、氛围、规则、重要事件与可用场景。 |
| 物品 | 来源、能力、限制、持有者与后续用途。 |
| 功法 / 能力 | 触发条件、效果、代价、等级和克制关系。 |

### 3.3 人物关系图

关系图支持添加人物节点、拖拽调整位置、缩放和平移、自动布局以及关系箭头。可选的箭头样式包括 `→`、`←` 和 `⇋`，分别适合表达单向、反向或双向关系。完成后可导出 PNG 或 SVG 文件，用于整理设定或对外展示。

新建关系图前，建议先在“设定”中建立人物卡；关系图可复用人物信息，并支持批量添加已有角色。

### 3.4 伏笔、时间线与灵感

“伏笔”用于记录线索的埋设和回收状态；“时间线”用于按故事内时间排列事件；“灵感”用于暂存句子、桥段或人物设想。三者都可作为长期创作中的辅助资料，但不会默认发送给 AI。

如果要让 AI 参考人物、世界设定和伏笔，请在 AI 侧栏中主动勾选“引用作品设定”。该选项会发送人物、世界设定与伏笔的限量摘要。

---

## 4. 搜索、替换、检查与导出

### 4.1 全局搜索与替换

使用 `Ctrl + Shift + F` 打开全局搜索。搜索范围可选择全部作品、当前全书、当前卷或当前章节。正文批量替换支持相同范围，并会显示替换统计与二次确认。

> 重要：批量替换是破坏性操作。软件会在替换前创建“操作前备份”，但仍建议先使用搜索结果确认匹配范围，尤其是角色名、称谓和专有名词。

### 4.2 内容检查

可在编辑器中检查当前章节，也可在作品总览中执行全本检查。检查功能包含内置敏感词示例和常见错别字词对；你可以在“设置”中补充投稿平台要求的自定义敏感词与错别字对。

| 检查方式 | 适用场景 |
|---|---|
| 当前章节检查 | 写完一章后快速检查。 |
| 全本检查 | 投稿、完本整理或集中修订前。 |
| 自定义词表 | 平台审核词、作品专用名称、容易写错的人名或地名。 |

### 4.3 导出作品

导出页面可导出 TXT、DOCX、PDF、EPUB、HTML、RTF、Markdown、单章 TXT 和 JSON 备份等格式。投稿或跨设备迁移时，优先使用 TXT、DOCX 或 JSON 全量备份。

| 目标 | 建议格式 |
|---|---|
| 纯文本投稿 | TXT。 |
| Word / WPS 编辑 | DOCX 或 RTF。 |
| 电子书阅读 | EPUB。 |
| 网页发布或预览 | HTML。 |
| 长期数据迁移与灾难恢复 | JSON 全量备份。 |

---

## 5. 自动备份、手动备份与恢复

### 5.1 自动备份策略

软件同时提供日常快照和关键操作保护。自动快照仅保存在本机数据库中；如果电脑损坏、系统重装或需要迁移设备，应再导出 JSON 文件并保存到另一块磁盘或可信云盘。

| 备份类型 | 触发时机 | 默认保留策略 |
|---|---|---|
| 每日备份 | 每天首次启动应用时。 | 最近 7 份。 |
| 定时快照 | 活跃写作期间，按设置间隔且内容有变化时。 | 与退出前、操作前备份合计最近 30 份。 |
| 退出前备份 | 关闭前距离最近保护备份超过 10 分钟且内容有变化时。 | 与定时、操作前备份合计最近 30 份。 |
| 操作前备份 | 导入、恢复备份、批量替换等重要操作前。 | 与定时、退出前备份合计最近 30 份。 |
| 手动备份 | 在“设置 → 数据备份”点击“立即备份”。 | 纳入本机备份列表。 |

在“设置 → 自动备份”中，你可以调整“定时快照间隔”和“定时 / 退出 / 操作前备份保留数量”。定时快照的可设置范围为 5 至 240 分钟；保留数量的可设置范围为 5 至 100 份。

Windows EXE 版还提供“外部自动导出”。在“设置 → 外部自动导出”中，选择一个文件夹并开启功能，再设置导出间隔天数（1 至 365 天）。软件在运行中或下一次启动后检查是否到期，到期时会将完整 JSON 备份写入该文件夹；软件关闭期间不会在后台运行。可使用“立即导出到该文件夹”测试文件夹权限。

### 5.2 导出 JSON 全量备份

进入“设置 → 数据备份”，点击“导出全部数据(JSON)”。浏览器或 EXE 会提示选择保存位置。建议文件名中保留日期，并至少保存到两个位置，例如电脑本地磁盘和个人云盘。

JSON 全量备份包括作品、卷、章节、人物、设定、大纲、伏笔、时间线、灵感、章节版本和大部分应用设置；**不包含 AI API Key**。

### 5.3 恢复本机备份

“设置 → 数据备份”列表中显示各类本机备份。点击“恢复”会用该备份覆盖当前全部数据。恢复前软件会先建立一份“操作前备份”，因此可在恢复结果不符合预期时回到恢复前状态。

恢复前请确认备份时间、类型和内容范围。恢复会覆盖当前本机作品、章节和设定，不能只恢复单个章节；若只需找回单章旧内容，请优先使用章节版本历史。

### 5.4 导入外部 JSON 备份

点击“导入数据”并选择墨阁导出的 JSON 文件后，可选择以下模式。

| 模式 | 行为 | 适用情形 |
|---|---|---|
| 合并导入 | 仅写入本机没有的记录，不覆盖已有记录。 | 从另一台设备补充新作品或新章节。 |
| 覆盖导入 | 先清空当前数据，再按备份完整恢复。 | 更换电脑、整体回退或从可信备份恢复。 |

无论选择哪种导入方式，软件都会先建立操作前备份。执行覆盖导入前，仍建议额外导出一次当前 JSON 备份。

---

## 6. AI 辅助写作

### 6.1 打开与隐藏 AI 侧栏

在任意章节编辑器中，点击工具栏的“✨ AI”即可显示或隐藏右侧 AI 侧栏。侧栏右上角的关闭按钮也可以隐藏它；隐藏后正文编辑区会自动扩大。

AI 用于提供续写、细纲扩展、润色和对白建议。它不会自动写入正文，生成结果会显示在侧栏中；你可以先复制检查，也可以点击“插入正文”插入到当前光标位置或替换当前选区。

### 6.2 配置 DeepSeek API

v1.6.0 首次打开 AI 配置时已默认填写 DeepSeek 参数。你只需提供自己的 API Key。

| 配置项 | DeepSeek 默认值或建议 |
|---|---|
| 基础地址 | `https://api.deepseek.com` |
| 模型名称 | `deepseek-chat` |
| API Key | 填写你在 DeepSeek 平台创建的 API Key。 |
| 创造度 | 默认 `0.8`；较低数值更稳定，较高数值更发散。 |

操作步骤如下：打开章节编辑器，点击“✨ AI”，再点击“配置档案”；确认基础地址和模型名称，粘贴 API Key，最后点击“保存当前档案”。配置后选择任务类型，再点击“生成写作建议”。

你可以在同一配置窗口新建多个 API 档案，例如“DeepSeek 日常写作”“豆包创作”“本地测试”。每个档案单独保存基础地址、模型和本机 API Key；切换档案不需要重新输入其他档案的 Key。

### 6.3 使用豆包或其他兼容 API

软件支持兼容 OpenAI Chat Completions 的服务。使用其他服务时，在“配置 API”中同时替换基础地址、模型名称和该服务对应的 API Key。

以豆包的火山方舟接口为例，基础地址可填写 `https://ark.cn-beijing.volces.com/api/v3`；模型名称必须填写你在方舟控制台已开通模型的准确 Model ID；API Key 应填写方舟创建的 `ARK_API_KEY`。软件会在基础地址后请求 `/chat/completions`。

> 当前版本支持多个本机 API 档案。每个档案拥有独立 Key；切换服务商时只需切换档案，无需覆盖其他档案的 Key。

### 6.4 AI 任务类型

| 任务 | 输入来源 | 生成结果 |
|---|---|---|
| 续写正文 | 当前章节细纲、备注和正文末段。 | 约 180 至 320 字的可插入正文建议。 |
| 扩展细纲 | 当前章节细纲、备注和正文末段。 | 4 至 6 条情节推进要点。 |
| 润色选段 | 当前章节细纲、备注与用户选中的正文。 | 保持事实、人称和称谓的润色文本。 |
| 生成对白 | 当前章节细纲、备注和正文末段。 | 带必要动作描写的对白建议。 |

使用“润色选段”前，请先在正文编辑区选中需要润色的文本；若没有选中文本，软件会提示先选择内容。

### 6.5 上下文引用与隐私控制

AI 侧栏的“上下文引用（可选）”默认只使用当前章节。你可以点击“选择引用章节”，从当前作品中按需勾选任意其他章节，最多选择 6 章；也可独立勾选“全书大纲”和“作品设定”。所有额外引用默认关闭，只有你主动选择的项目才会随 AI 请求发送。

| 内容 | 默认是否发送 | 勾选后发送内容 | 长度控制 |
|---|---|---|---|
| 当前章节标题、细纲、备注 | 是。 | 当前编辑章节的信息。 | 按当前内容发送。 |
| 当前正文 | 是。 | 续写、细纲或对白时发送正文末段；润色时发送选中段落。 | 正文末段最多约 5,000 个字符。 |
| 用户选择的章节 | 否。 | 你在章节范围窗口中选择的任意章节的细纲、备注、标签和正文摘录。 | 最多 6 章；每章自动截取摘要。 |
| 全书大纲 | 否。 | 大纲文档内容。 | 最多约 5,000 个字符。 |
| 作品设定 | 否。 | 人物、世界设定与伏笔摘要。 | 最多约 6,000 个字符。 |

侧栏会显示“本次额外发送”的范围。若不希望向 API 服务发送跨章节或设定内容，请不要选择额外章节，也保持大纲和设定选项未勾选。

### 6.6 API Key 安全

API Key 只保存在本机，不写入作品、章节、JSON 备份、源码包或 Release 附件。不要把 API Key 发送给他人，也不要分享整个应用数据目录。

如果 Key 被他人获取，对方可能使用你的 API 账户产生调用消耗。发生泄露时，应立即前往相应平台删除或轮换该 Key，然后回到软件中保存新 Key。

---

## 7. 外观与编辑偏好

在“设置”页面可调整主题、字体、字号、行距、自动保存间隔和默认每日目标。设置会自动保存到本机。

| 设置项 | 建议 |
|---|---|
| 主题 | 长时间夜间写作可使用深色；白天可使用浅色或跟随系统。 |
| 正文字体 | 小说正文通常适合宋体或楷体；标题与界面字体不受正文选择影响。 |
| 字号与行距 | 初始可使用 18px 与 2.0 行距，再按显示器和习惯微调。 |
| 自动保存间隔 | 1 秒适合大多数情况；不建议设置过长。 |
| 每日目标 | 设置为可持续完成的字数，配合统计页观察连续写作情况。 |

---

## 8. 常见问题

### 8.1 关闭软件会保存吗？会自动备份吗？

关闭时软件会尝试保存尚未落盘的当前编辑内容。自动备份并不是每次关闭都创建：每天首次启动会创建每日备份；活跃写作期间会按定时快照间隔备份；关闭前只有在距离最近保护备份超过 10 分钟且存在改动时，才创建退出前备份。

### 8.2 为什么导出的 JSON 里没有 API Key？

这是设计上的安全措施。作品备份用于迁移和恢复创作数据，不应携带可产生账户消耗的 API Key。新电脑或其他用户需要自行配置 API Key。

### 8.3 把 EXE 和作品备份发给别人后，对方能使用我的 AI 吗？

不能。EXE、源码、作品配置和 JSON 备份不包含你的 API Key。对方必须使用自己的 Key。请不要向他人发送 API Key 或整个应用数据目录。

### 8.4 AI 返回 401、403 或生成失败怎么办？

先确认基础地址、模型名称和 API Key 是否与服务商控制台信息一致。使用豆包时应使用方舟 `ARK_API_KEY`，而不是 Access Key / Secret Key。使用其他服务商时，请确认其支持 OpenAI Chat Completions 兼容接口以及 `Bearer` API Key 鉴权方式。

### 8.5 AI 不知道前后剧情怎么办？

在“上下文引用（可选）”中点击“选择引用章节”，再按需勾选与当前任务相关的章节，也可选择“全书大纲”或“作品设定”。不要默认选择过多内容；只选择当前任务真正需要的信息，可减少发送内容并提高聚焦度。

### 8.6 如何迁移到另一台电脑？

在旧电脑中执行“设置 → 数据备份 → 导出全部数据(JSON)”，将生成的 JSON 文件复制到新电脑。在新电脑安装或运行软件后，进入同一位置点击“导入数据”，根据需要选择“合并导入”或“覆盖导入”。AI API Key 不会迁移，需要在新电脑重新配置。

### 8.7 如何找回误删或误替换的内容？

先查看“设置 → 数据备份”中的操作前、定时或手动备份；如果只影响单一章节，优先使用该章节的“历史版本”进行比较和回滚。执行恢复前请先确认备份时间，并在必要时额外导出当前 JSON 备份。

---

## 9. 建议工作流

对于长篇网络小说，推荐先建立作品资料，再按卷和章节推进。每章开始前在“本章细纲”写明目标、冲突、转折和钩子；正文完成后检查错别字与敏感词，保存章节版本；达到阶段节点后，导出 JSON 备份到外部位置。

AI 应作为辅助而非唯一创作来源。需要续写时，先完成当前章细纲与写作备注；需要跨章一致性时，再明确选择相关引用范围；生成结果插入正文前，仍应人工核对人物称谓、时间线、设定和语气是否正确。

---

## 10. 数据与隐私摘要

| 类别 | 默认处理方式 |
|---|---|
| 作品与正文 | 保存在本机。 |
| 自动备份 | 保存在本机数据库。 |
| JSON 导出 | 由用户选择保存位置。 |
| API Key | 单独保存在本机，不写入作品、备份或源码。 |
| AI 请求 | 只有用户主动点击生成时才发起；发送当前章上下文及用户勾选的额外引用。 |
| 账号与统计 | 软件不提供账号系统，也不进行统计上报。 |

如需技术支持或查看最新版发布文件，请访问项目主页：<https://github.com/lily-XYL/moge-writer>。
